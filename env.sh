THIS_DIR=$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )

# ---------------- EDIT THIS ------------------

#The line below must be configured.
BMV2_PATH=$THIS_DIR/../bmv2 
# BMV2_PATH=$THIS_DIR/bmv2 - if the repo is cloned in this directory

#The line below must be configured.
P4C_BM_PATH=$THIS_DIR/../p4c-bmv2 
# P4C_BM_PATH=$THIS_DIR/p4c-bmv2 - if the repo is cloned in this directory

# ---------------- END ------------------
